[project]
name = "sqlguard"
version = "0.1.0"
description = "SQL safety gate between an LLM and a customer's Microsoft SQL Server"
requires-python = ">=3.10"
dependencies = ["sqlglot>=25.0"]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
